'use strict';

/**
 * photographer-profile router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::photographer-profile.photographer-profile');
