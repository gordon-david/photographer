'use strict';

/**
 * photographer-profile controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::photographer-profile.photographer-profile');
